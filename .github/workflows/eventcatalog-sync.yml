name: Notify EventCatalog

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger EventCatalog sync
        env:
          EVENTCATALOG_TOKEN: ${{ secrets.INTEGSHOP_GH_TOKEN }}
        run: |
          curl -s -X POST \
            -H "Authorization: token $EVENTCATALOG_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/integshop/zynchub-event-catalog/dispatches \
            -d '{"event_type":"org-repo-push"}'

